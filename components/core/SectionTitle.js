
const SectionTitle = ({ title }) => {
  return (
    <div style={{ textAlign: "center", textDecoration: "underline", fontSize: "45px", lineHeight: "54px", margin: '1em' }}>{title}</div>
  )
}

export default SectionTitle